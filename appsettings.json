{
  "ConnectionStrings": {
    "SqlServer": "Server=${SQLSERVER_HOST};Database=${SQLSERVER_DATABASE};User Id=${SQLSERVER_USER};Password=${SQLSERVER_PASSWORD};",
    "PostgreSql": "Host=${POSTGRES_HOST};Database=${POSTGRES_DATABASE};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD};"
  },
  "AppSettings": {
    "ExecutionIntervalMinutes": 60,
    "PeriodSelection": 90
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
